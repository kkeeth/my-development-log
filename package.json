{
  "name": "my-blog",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "init": "honkit init",
    "build": "rm -rf docs && honkit build && mv _book docs",
    "serve": "honkit serve"
  },
  "dependencies": {
    "honkit": "^5.1.4"
  },
  "devDependencies": {
    "gitbook-plugin-chapter-fold": "^0.0.4",
    "gitbook-plugin-folding-chapters": "^0.0.9",
    "gitbook-plugin-sidebar-ad": "^0.0.4"
  }
}
